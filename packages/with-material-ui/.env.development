# DO NOT ADD SECRETS TO THIS FILE. This is a good place for defaults.
# If you want to add secrets use `.env.development.local` instead.

# env vars with NEXT_PUBLIC_ prefix are available both in server and browser
NEXT_PUBLIC_HTTP_SERVER_HOST="https://countries.trevorblades.com"

# env vars without NEXT_PUBLIC_ prefix are only available in server
ENV_VARIABLE="server_only_development_variable"
